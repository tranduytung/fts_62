<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">
      <%= t "question.question", content: question.content %>
      <br><br>
      <%= t "question.type" %>:
      <%= t question.question_type %>
      <br>
      <%= t "question.status" %>:
      <%= t question.status %>
    </h3>
  </div>
  <hr>
  <div>
    <% question.answers.each do |answer| %>
      <div class ="checkbox">
        <label>
          <%= check_box_tag answer.content, answer.is_correct? ?
            "yes" : "no", true, disabled: true %>
          <%= answer.content %>
        </label>
      </div>
    <% end %>
  </div>
</div>
